# إعداد قاعدة بيانات تحكم تطبيق ولي الأمر

1. افتح Supabase.
2. افتح SQL Editor.
3. افتح الملف `supabase/V121_PARENT_CONTROL_DATABASE_SETUP.sql`.
4. انسخ محتواه بالكامل وشغله مرة واحدة.
5. أعد تحميل صفحة الإدارة.

الملف آمن للتشغيل أكثر من مرة لأنه يستخدم `create table if not exists`.
